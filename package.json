{
  "name": "mountain-breeze-villa",
  "private": true,
  "version": "1.0.0",
  "description": "Mountain Breeze Villa – Full-stack eco hotel website",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "create-admin": "npm run create-admin --prefix backend"
  }
}
